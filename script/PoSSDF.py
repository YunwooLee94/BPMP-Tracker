

def PoSSDF(br0, bt0, u0, obstacles, fov, params):
    """
    Sequential Convex Programming for PoSSDF (Algorithm 2 in the paper)
    Inputs:
        br0: initial robot belief (Belief object)
        bt0: initial target belief (Belief object)
        u0: initial control sequence (N x 2 array)
        obstacles: list of Obstacle objects
        fov: FOV object
        params: Params object
    Outputs:
    """
    pass


    
def PoSSDF_LOS(br0, bt0, u0, obstacles, fov, params):
    """
    Sequential Convex Programming for PoSSDF (Algorithm 2 in the paper)
    Inputs:
        br0: initial robot belief (Belief object)
        bt0: initial target belief (Belief object)
        u0: initial control sequence (N x 2 array)
        obstacles: list of Obstacle objects
        fov: FOV object
        params: Params object
    Outputs:
    """
    pass